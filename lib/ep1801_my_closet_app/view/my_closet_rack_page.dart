import 'package:flutter/material.dart';

class MyClosetRackPage extends StatelessWidget {
  const MyClosetRackPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
