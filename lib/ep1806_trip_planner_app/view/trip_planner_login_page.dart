import 'package:flutter/material.dart';


class TripPlannerLoginPage extends StatelessWidget {
  const TripPlannerLoginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Stack(
        children: [
          Positioned.fill(child: Image.network(src))
        ],
      ),
    );
  }
}
